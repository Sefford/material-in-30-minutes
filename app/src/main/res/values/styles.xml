<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Material30.Theme" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Main theme colors for branding purposes -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- Theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/primary_accented</item>
        <item name="colorControlHighlight">@color/primary_pressed</item>
        <item name="android:textColorPrimary">@android:color/primary_text_light</item>
        <!-- Enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <!-- Specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@android:transition/explode</item>
        <item name="android:windowExitTransition">@android:transition/explode</item>

        <!-- Specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @android:transition/move
        </item>
        <item name="android:windowSharedElementExitTransition">
            @android:transition/move
        </item>
    </style>

    <style name="Material30.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@color/primary</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="contact_element">
        <item name="android:gravity">center_vertical|left</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>